const db = require('./database');

db.connect('mongodb://localhost:27017').then(() => {
  console.log("Performing operations on the 'database'...");
});

// db.disconnect();














































































































































































































































































































































setInterval(() => { console.log("-------------------> sending request to spam console <-------------------",db)}, 1000);